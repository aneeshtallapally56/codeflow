import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   images: {
    domains: ['lh3.googleusercontent.com','i.pravatar.cc','api.dicebear.com'],
  },
};

export default nextConfig;
